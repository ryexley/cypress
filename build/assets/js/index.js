// TODO: consider replacing parallax.js with a simple function similar to:
// https://github.com/juanpablob/santiagum/blob/master/assets/js/santiagum.js#L15

(function ($, global) {

    var app = global.Cypress = global.Cypress || {

    };

    global.Cypress = app;

}(jQuery, this));
